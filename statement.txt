back office teams receive large volumes of documents daily that need to be classified and routed to appropriate departments, which is time consuming and error-prone when done manually. This is our problem statement steps which we need to follow to acheive this: Use a llm to analyze document content Develop an ai agent that callsifies documents into predefined categories (e.g. invoices, contracts, reports0. Automatically suggest routing instructions based on classification provide a simple interface for manual overrides. data requirements: sample documents in text format representing various back office documents types, possibly anonymized or synthetic expected output: a classification label and routing suggestion for each document in text or json format